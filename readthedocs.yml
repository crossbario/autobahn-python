version: 2

python:
   version: 3.8
   #install:
   #   - requirements: requirements-rtd.txt
   install:
      - method: pip
        path: .
        extra_requirements:
          - dev
          - twisted
          - compress
          - serialization
          - encryption
          - scram
